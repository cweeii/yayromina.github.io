import { IndexRoute, Route } from 'react-router';

import {
  HomeView,
} from './views';
import { MainLayout } from 'imports/components/layouts';

const routes = props => (
  
);

export default routes;
